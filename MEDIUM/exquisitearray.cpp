#include <iostream>
#include <vector>

using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int n;
        vector<int> per_order;
        cin >> n;
        for(int i = 0; i < n; i++){
            int x;
            cin >> x;
            per_order.push_back(x);
        }

    }
}